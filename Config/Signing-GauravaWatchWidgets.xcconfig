// Maintainer-local Release signing values (team ID, provisioning profile)
// live in the gitignored Signing-GauravaWatchWidgets.local.xcconfig. Forks build without
// one: simulator lanes need no signing; the include below is optional.
#include? "Signing-GauravaWatchWidgets.local.xcconfig"
