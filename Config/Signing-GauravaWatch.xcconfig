// Maintainer-local Release signing values (team ID, provisioning profile)
// live in the gitignored Signing-GauravaWatch.local.xcconfig. Forks build without
// one: simulator lanes need no signing; the include below is optional.
#include? "Signing-GauravaWatch.local.xcconfig"
